@model Pry_Agencia_Viajes.Ciudad

@{
    ViewBag.Title = "Delete";
}

<div class="super_container">

    @Html.Partial("Cabezera_Ciudad", "Eliminar")
    <div class="intro jumbotron">
        <div class="container">
            <h3>¿Está seguro de eliminar la Ciudad "@Html.DisplayFor(model => model.ciu_nombre)" ?</h3>
            <div>
                @Html.DisplayNameFor(model => model.ciu_descripcion)
            </div>
            <div>
                @Html.DisplayFor(model => model.ciu_descripcion)
            </div>
            <br />
            @using (Html.BeginForm())
            {
                @Html.AntiForgeryToken()

                <div class="">
                    <input type="submit" value="Si Eliminar" class="btn btn-primary" /> 
                    <a class="btn btn-danger" href="@Url.Action("Index")">Cancelar</a>
                </div>
            }
        </div>
    </div>
</div>

     